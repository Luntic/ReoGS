MongoDB connection string (replace with your MongoDB URI)

MONGODB_URI=mongodb://localhost/reo

HTTP server port

HTTP_PORT=8080

HTTPS server port

HTTPS_PORT=8443

Path to SSL private key

SSL_KEY_PATH=server.key

Path to SSL certificate

SSL_CERT_PATH=server.cert

Secret key for session management (generate a strong random string)

SESSION_SECRET=your-secret-key-here

Optional: API key for external services (e.g., custom auth)

API_KEY=your-api-key-here